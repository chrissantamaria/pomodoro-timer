import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  timerRow: {
    paddingBottom: 30
  },
  time: {
    fontSize: 120
  },
  button: {
    margin: "0 10px"
  }
}));

export default useStyles;
